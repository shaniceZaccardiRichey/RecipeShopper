using Microsoft.AspNetCore.Http;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace RecipeShopper.Models
{
    public class RecipeSession
    {
        /*
        private const string RecipeKey = "recipe";

        private ISession session { get; set; }
        public MySession(ISession = sess) {
            session = sess;
        }

        public List<Recipe> GetRecipes(List<namespace> recipe) =>
            session.GetObject( );
        */
    }
}
